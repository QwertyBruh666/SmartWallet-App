export type ChartInfoObject = {
    x: number,
    o: number,
    h: number,
    l: number,
    c: number
}

export type ChartOptions = {
    interaction: object,
    plugins: object,
    scales: object
}